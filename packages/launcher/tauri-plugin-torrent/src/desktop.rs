use serde::de::DeserializeOwned;
use tauri::{plugin::PluginApi, AppHandle, Runtime};

use crate::models::*;

pub fn init<R: Runtime, C: DeserializeOwned>(
  app: &AppHandle<R>,
  _api: PluginApi<R, C>,
) -> crate::Result<Torrent<R>> {
  Ok(Torrent(app.clone()))
}

/// Access to the torrent APIs.
pub struct Torrent<R: Runtime>(AppHandle<R>);

impl<R: Runtime> Torrent<R> {
  pub fn ping(&self, payload: PingRequest) -> crate::Result<PingResponse> {
    Ok(PingResponse {
      value: payload.value,
    })
  }
}
